#include<cstdio>
#include<cmath>
#include<cstring>
#include<algorithm>
using namespace std;
const int mod = 1e9 +7;

int main() {
	int T, a, b, n;
	scanf("%d", &T);
	while(T--) {
		scanf("%d %d %d", &a, &b, &n);
		
	}
	return 0;
}
